<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SplashActivity"
    >

    <ImageView
        android:layout_width="400dp"
        android:layout_height="400dp"
        android:id="@+id/ivSplashScreenLogo"
        android:src="@drawable/splash_screen_logo"
        android:layout_centerInParent="true" />

    <!--<TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Institute of Engineering"
        android:textSize="25sp"
        android:textStyle="bold"
        android:id="@+id/tvSplashScreenTitle"
        android:layout_below="@id/ivSplashScreenLogo"
        android:textAlignment="center"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="and Technology"
        android:textSize="25sp"
        android:textStyle="bold"
        android:id="@+id/tvSplashScreentitle"
        android:layout_below="@+id/tvSplashScreenTitle"
        android:textAlignment="center"
        />-->

</RelativeLayout>